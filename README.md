# JsonTape

__JsonTape__ is a minimalistic allocator-aware JSON parser in pure Rust.
It's built to address the high memory usage & fragmentation as well high traversal latency in typical DOM implementations.
It's sibling project - [StringTape](github.com/ashvardanian/StringTape), does the same for Arrow-like string collections.
